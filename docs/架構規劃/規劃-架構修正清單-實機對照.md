# 規劃 · 架構修正清單（實機對照）

> 2026-07 維運期間於 `.166` 實際執行的修正。狀態：✅ 已完成。皆有備份。

---

## A. macOS 移轉殘留（根因類）

| # | 問題 | 原本（macOS） | 修正 | 檔案 | 備份 |
|---|------|--------------|------|------|------|
| A1 | **時區錯位** | 系統停 UTC，cron 用系統時區判讀 → 每日更新實跑台北凌晨 01:00、且少抓一天 | `timedatectl set-timezone Asia/Taipei` + restart cron | 系統層 | — |
| A2 | **服務檢測失效** | 監控頁用 `launchctl`（macOS 專用） | 改查 cron/systemd | `dashboard/pages/monitor.py` | `.bak_mac` |
| A3 | **硬編碼路徑** | `~/Desktop/Stock/…`（Mac 桌面） | 動態 `PROJECT_ROOT` | `monitor.py` | `.bak_mac` |
| A4 | **手動更新指令錯** | `python3 scripts/hourly_update.py`（不存在） | `bash scripts/hourly_data_update.sh` | `monitor.py` | `.bak_mac` |

## B. 指錯資料表（schema 契約類）

| # | 問題 | 原指向 | 改為 | 檔案 |
|---|------|--------|------|------|
| B1 | 財報覆蓋率 503%→65% | `financial_reports`(204檔) | `quarterly_earnings`(1984檔) | `monitor.py` |
| B2 | 總覽財報記錄 | `financial_reports` | `quarterly_earnings` | `home.py` |
| B3 | 財報摘要頁查無 6811 | `financial_reports` | `quarterly_earnings`（整頁重寫） | `financials.py` |

## C. UI / 資料呈現

| # | 問題 | 修正 | 檔案 |
|---|------|------|------|
| C1 | 覆蓋率 503%（分母子集/分子全集） | 改取交集（上市櫃池 ∩ 有資料） | `monitor.py` |
| C2 | 選股下拉 15,336 檔卡死瀏覽器 | 改文字輸入 + 上市櫃池 2422 檔 | `charts.py` |
| C3 | 本益比顯示「-」 | 改從 `stock_factors.pe_ratio` 讀（非 stock_price） | `charts.py` |
| C4 | 回測頁寫死 2024 示範 | 改互動式：選策略/股票池/期間現場跑 | `backtest_viz.py` |
| C5 | 側邊欄雙重導航 | `config.toml` 關自動 pages 導航 | `.streamlit/config.toml` |
| C6 | 缺 psutil crash | `pip install psutil` | — |

## D. 團隊分析管線（邏輯類）

| # | 問題 | 修正 | 檔案 |
|---|------|------|------|
| D1 | phase2 檔名用當天日期，跨午夜找不到 phase1 存檔 | 加 `--date` + 退回最新一份 | `team_daily_verified.py` |
| D2 | 合議平手依 dict 順序偏「買進」 | 平手改採顧問草案評級、否則保守持有 | `consensus.py` |
| D3 | 投票理由抽到樣板字「【我的判斷】」 | 跳過純標題行取實質理由 | `consensus.py` |
| D4 | 復驗慢層把「DB無資料」誤標背離 | 新增「DB無資料」類別、FinMind 0.0 視為無交易、精確日期匹配 | `reverify_team.py` |

## E. 資料補正（一次性）

| # | 內容 | 動作 |
|---|------|------|
| E1 | 07-09 stock_price 只剩 TAIEX 1 筆 | 重跑 `twse_daily_update` 補齊 6044 筆 |
| E2 | 07-08 TPEX 股價缺 | 新站台端點補 892 檔 |
| E3 | 團隊分析 720 檔用過期股價 | 就地重跑補正（stale 720→0） |

## F. 新增能力（見 [規劃-推薦模組選型](規劃-推薦模組選型.md)）
- `team_analysis` 集合 + 雙寫 + migration + 兩層復驗
- `/api/team` `/api/team/{symbol}` 端點
- 儀表板：團隊分析、每日選股、互動回測三頁
- CLI：`query_team.py`
- dashboard systemd 化（`twstock-dashboard.service`，綁 0.0.0.0）

---

## 版本分歧提醒
上述 D/F 的新檔（`team_store.py`, `reverify_team.py`, `migrate_team_to_db.py`, `query_team.py`, 新儀表板頁）**只在 `.166`**，本機 Windows bundle 尚未同步。需 pull 回本機或以 `.166` 為權威。
