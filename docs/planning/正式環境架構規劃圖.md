# 正式環境架構規劃圖 — tw-stock-analysis

> 環境：`ming-ai02` (172.16.9.166) + GPU 推理節點 .27/.28。基於實機盤點（systemd/ss/cron/.env）繪製。
> 部署版本：`89705ce`（Phase 0-2 + 修復）。日期基準：2026-09-22 上線。

## 1. 實體拓撲與網路安全邊界
```mermaid
flowchart TB
  subgraph EXT["🌐 外部資料源 / 通道"]
    FIN[FinMind API]
    TWSE[TWSE OpenAPI]
    TDCC[TDCC 集保]
    YH[Yahoo Finance]
    GN[Google News RSS]
    LINE[LINE Notify]
  end

  subgraph LAN["🏠 內網 172.16.9.0/24"]
    subgraph APP["🖥️ ming-ai02 · 172.16.9.166（應用主機）"]
      DASH["twstock-dashboard.service<br/>Streamlit :8501 · 0.0.0.0（對外）"]
      API["twstock-api.service<br/>FastAPI :8888 · 127.0.0.1（僅內部）"]
      MONGO[("MongoDB :27017 · 127.0.0.1<br/>tw_stock_analysis · ~65 集合")]
      CRON["cron（19+ 排程）<br/>資料同步腳本"]
      VENV["/home/mdsadmin/Stock/.venv<br/>Python 3.12"]
    end
    subgraph GPU27["🎮 172.16.9.27（GPU 節點·合議）"]
      OL27["Ollama :11434<br/>qwen2.5-14b · llama3.1:8b"]
    end
    subgraph GPU28["🎮 172.16.9.28（GPU 節點·主力）"]
      OL28["Ollama :11434<br/>qwen3-14b · gemma2:9b"]
    end
  end

  USER([使用者/瀏覽器]) -->|:8501 HTTP| DASH
  ADMIN([維運/SSH]) -->|:22| APP
  DASH -->|localhost :8888| API
  DASH & API --> MONGO
  DASH -->|RAG/國際新聞| OL28
  CRON --> MONGO
  CRON -->|抓取| FIN & TWSE & TDCC & YH
  CRON -->|告警| LINE
  DASH -->|網路補充| GN
  APP -.MoE 推理.-> OL27 & OL28

  classDef exposed fill:#ffe0e0,stroke:#c00
  classDef internal fill:#e0f0ff,stroke:#06c
  class DASH exposed
  class API,MONGO internal
```
> **安全邊界（實測 `ss -tlnp`）**：只有 `SSH(22)` 與 `Dashboard(8501, 0.0.0.0)` 對外；
> `API(8888)` 與 `MongoDB(27017)` 綁 `127.0.0.1` **僅內部可達**；Ollama 節點限內網。

## 2. 服務與資料流（主機 .166 內）
```mermaid
flowchart LR
  U([使用者]) --> DASH[Streamlit 儀表板]
  DASH -->|REST| API[FastAPI 12 端點<br/>/api/price·factors·valuation·risk·<br/>ranking·score·macro·sentiment·predict…]
  API --> REPO[analysis 模組<br/>ValuationAnalyzer/StockRanker/<br/>RiskAnalyzer/…]
  REPO --> MONGO[(MongoDB<br/>tw_stock_analysis)]
  DASH --> RAG[rag_page<br/>向量檢索 bge-m3 + 字面 RRF]
  RAG --> MONGO
  RAG --> WS[web_search<br/>Google News RSS]
  RAG --> IN[international_news<br/>+ EvidenceGuard 守門]
  IN --> OL[(Ollama .28)]
  DASH --> CFG[src/config.py<br/>MONGODB/OLLAMA/路徑 單一真相源]
  API --> CFG
```

## 3. 資料匯入管線（cron 排程）
```mermaid
flowchart TB
  subgraph SCHED["⏰ cron（/home/mdsadmin/Stock/.venv）"]
    H["每時 :05 hourly_data_update.sh"]
    D1["每日 01:00 dividend / 01:30 adjustment_factors<br/>02:00 adj_close backfill"]
    D2["工作日 17:10 macro_sync · 17:30 twse_openapi_sync"]
    M["每月 revenue / quarterly_earnings / outstanding_shares"]
    W["每週日 mongodb_backup · shares · log rotation"]
  end
  SRC[FinMind / TWSE OpenAPI / TDCC / Yahoo]
  SCHED -->|抓取·清洗·upsert| SRC
  SCHED --> MONGO[(MongoDB ~65 集合)]
  SCHED -->|排程警報| ALERT[(schedule_alerts)]
  SCHED -->|失敗通知| LINE[LINE Notify]
  MONGO --> ALERT
```

## 4. MoE / 多模型 AI 推理拓撲
```mermaid
flowchart TB
  Q[分析請求/團隊分析] --> RR[role_router<br/>7 角色 → 模型/節點]
  RR -->|technical/risk| OL27["Ollama .27<br/>llama3.1:8b · qwen2.5-14b"]
  RR -->|advisor/fundamental/<br/>macro/value/chip| OL28["Ollama .28<br/>qwen3-14b · gemma2:9b"]
  RR --> GUARD[🛡️ moe/guard<br/>usable_reports：失敗回應不進整合]
  GUARD --> ADV[investment-advisor 整合草案]
  ADV --> CONS[consensus.discuss<br/>3 委員序列討論 + 主持人定案]
  CONS -->|盲投/棄權守門| OL27 & OL28
  CONS --> TA[(team_analysis 集合)]
  style GUARD fill:#ffe0e0
```
> Phase 0 新增 `moe/guard`：角色 LLM 失敗回應（"分析失敗:…"）**不再計入**顧問整合與合議投票。

## 5. 部署與維運架構
```mermaid
flowchart LR
  subgraph GIT["原始碼管理"]
    LOCAL[開發機 repo] -->|git bundle| BUNDLE[bundle]
    BUNDLE -->|scp + ff-only| PROD[prod checkout<br/>/home/mdsadmin/Stock/tw-stock-analysis]
    PROD --> BK[backup/pre-phase02-* 分支<br/>可回滾]
  end
  subgraph RUN["執行"]
    SYS[systemd] --> SVCA[twstock-api.service]
    SYS --> SVCD[twstock-dashboard.service]
    SVCA & SVCD --> VENV[Stock/.venv Python 3.12]
  end
  subgraph CI["護欄（.github/workflows）"]
    C1[ruff lint]
    C2[pytest 174]
    C3[pip-audit CVE]
    C4[hardcode-gate]
    C5[gitleaks 機密掃描]
  end
  PROD --> SYS
```

## 6. 關鍵事實速查（實機盤點）
| 項目 | 值 |
|---|---|
| 應用主機 | ming-ai02 / 172.16.9.166 / Python 3.12 |
| 對外埠 | SSH 22、Dashboard 8501（0.0.0.0） |
| 內部埠 | API 8888、MongoDB 27017（皆 127.0.0.1） |
| GPU 推理 | .27（qwen2.5-14b/llama3.1）、.28（qwen3-14b/gemma2）Ollama :11434 |
| DB | MongoDB `tw_stock_analysis` ~65 集合 |
| 服務管理 | systemd：twstock-api / twstock-dashboard（Restart=on-failure） |
| 排程 | cron 19+（時/日/週/月資料同步 + 備份 + 告警） |
| 設定中心 | `src/config.py`（MONGODB/OLLAMA/路徑，env 覆寫） |
| 部署方式 | git bundle → ff-only + systemctl restart（backup 分支可回滾） |

## 7. 安全態勢摘要
- ✅ MongoDB / API 僅 `127.0.0.1`，未對外暴露。
- ✅ Ollama 限內網 GPU 節點。
- ✅ Phase 0 硬化：XSS 消毒、SSRF 白名單、提示注入/EvidenceGuard、MoE 決策守門。
- ✅ CI 護欄：ruff / pytest / pip-audit / 硬編碼 gate / gitleaks。
- ⚠️ Dashboard 8501 綁 `0.0.0.0`（內網對外）—— 建議評估加反向代理/SSO（Phase 3+）。
