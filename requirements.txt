# ================================
# 台股智能分析系統 - Python 依賴清單
# ================================
# 安裝: pip install -r requirements.txt
#
# [LEGACY] 本檔已非單一真相來源(SoT)；權威依賴宣告在 pyproject.toml。
#   正式安裝: pip install -e ".[dev]"  (儀表板加 .[dashboard]，TEJ 加 .[tej])
#   新增依賴請改 pyproject.toml,勿只改此檔(避免兩份再度分裂)。

# ── 核心 ──
numpy>=2.0
pandas>=2.0
scipy>=1.10
pymongo>=4.6

# ── 資料取得 ──
requests>=2.28
urllib3>=2.0
python-dotenv>=1.0
finmind>=1.9
tqdm>=4.60

# ── 視覺化 ──
matplotlib>=3.7
# streamlit        # Dashboard（可選）
# plotly           # Dashboard 互動圖表（可選）

# ── LINE 通知 ──
# (使用 requests，無額外依賴)

# ── ML 模型 ──
scikit-learn>=1.3
xgboost>=2.0

# ── 開發工具（可選）──
# pytest>=7.0
# pytest-cov>=4.0
